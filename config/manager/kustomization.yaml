# generatorOptions:
#   disableNameSuffixHash: true
resources:
- manager.yaml
# configMapGenerator:
# - files:
#   - controller_manager_config.yaml
#   name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: default-route-openshift-image-registry.apps.psx50.cp.fyre.ibm.com/open-liberty-operator/operator
  newTag: latest
