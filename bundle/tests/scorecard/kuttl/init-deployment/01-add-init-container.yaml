apiVersion: apps.openliberty.io/v1
kind: OpenLibertyApplication
metadata:
  name: init-liberty-deployment
spec:
  # Add fields here
  initContainers:
  - image: alpine:3.14
    command: ["echo"]
    args: ["Hello world"]
    name: init1

