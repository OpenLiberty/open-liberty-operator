apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-strategy-lib-app
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 2
status:
  replicas: 3
  readyReplicas: 3
  updatedReplicas: 3