apiVersion: apps.openliberty.io/v1beta2
kind: OpenLibertyApplication
metadata:
  name: deployment-strategy-lib-app
spec:
  # Add fields here
  replicas: 3
  deployment:
    updateStrategy:
      type: Recreate