apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-with-cert-lib-app
status:
  readyReplicas: 1
spec:
  template:
    spec:
      volumes:
      - name: svc-certificate
        secret:
          defaultMode: 420
          secretName: service-tls-secret
