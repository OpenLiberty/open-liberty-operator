apiVersion: apps.openliberty.io/v1
kind: OpenLibertyApplication
metadata:
  name: service-with-cert-liberty
spec:
  # Add fields here
  applicationImage: k8s.gcr.io/pause:3.2
  replicas: 1
  service:
    certificateSecretRef: service-tls-secret

