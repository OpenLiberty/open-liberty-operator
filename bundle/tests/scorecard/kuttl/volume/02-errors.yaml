apiVersion: apps/v1
kind: Deployment
metadata:
  name: volume-liberty
spec:
  template:
    spec:
      containers:
        - volumeMounts:
            - name: test-volume
      volumes:
        - name: test-volume