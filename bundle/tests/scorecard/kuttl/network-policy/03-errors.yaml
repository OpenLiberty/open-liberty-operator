apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: network-policy-liberty
spec:
  ingress:
    - ports:
        - port: 9090
          protocol: TCP
        - port: 9091
          protocol: TCP
