apiVersion: apps.openliberty.io/v1
kind: OpenLibertyApplication
metadata:
  name: network-policy-liberty
spec:
  expose: false
  networkPolicy:
    namespaceLabels:
      namespace: test
    fromLabels:
      foo: bar
  service:
    port: 8080
    ports:
      - port: 9090
        protocol: TCP
