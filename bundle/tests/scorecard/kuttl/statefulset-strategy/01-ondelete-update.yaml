apiVersion: apps.openliberty.io/v1beta2
kind: OpenLibertyApplication
metadata:
  name: statefulset-strategy-liberty
spec:
  # Add fields here
  replicas: 3
  statefulSet:
    updateStrategy:
      rollingUpdate: null
      type: OnDelete